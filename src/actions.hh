#pragma once

#include <string>

class Actions {
public:
	static void openFile();
	static void saveFile();
	static void saveFileAs();
	static void cut();
	static void copy();
	static void paste();
};
